function int fib(int x)
  if (x==1) 
    return 1
  else 
    if (x==2)
      return 1
    else
      y = fib(x-1)
      z = fib(x-2)
      k = y+z
      // can be changed to return expression in the future
      return k
    end
  end
end 

main()
  fib(3)
end
