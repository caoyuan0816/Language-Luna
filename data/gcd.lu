function int gcd(int x, int y)
  if (x==y)
    return x
  else
    if (x>y)
      int z=x-y
      return gcd(y,z)
    else
      z=y-x
      return gcd(x,z)
    end
  end
end

main()
  int a = 128
  int b = 64
  print(gcd(a,b))
end
