/**
 *grammar test
 *version 1.0
 */

//return larger value
function int test(int x, int y)
	int z = 0
	if (x>y)
	  z=x
	else
	  z=y
	end
	return z
end

//return the largest value in a positive list
function int max(list arr)
	int res = 0
	for x in arr do
	  if (x>res)
	    res = x
	  end
	end
	return res
end

//return index of largest value in a positive list
function int maxpos()
	int index = 0
	int res = 0
	int maxValue = 0
        for x in [1,3,5,2,4] do
	  if (maxValue<x)
	    maxValue = x
	    res = index
	  end
          index++
        end
	return res
end

//main
main(int argc, list argv)
	int x = 0
	int y = 0
	double z =0.0
	x= 0.25*9-4+y+z
	if (6*x==y+z) 
          x=y*6
	  int k = 0
	else
	  y=x+1
	  x++
	end
	for x = 1, 6, 1
	do
	  y = y+x
	end
	x=0
	while (x<10) do
	  x++
	end
        list arr = [2,3,4,5,7,1]
	int res = max(arr)
end
